SET(ALL_SAMPLES
	minimal
)

IF (FUSEPP_USE_POSTGRESQL)
	LIST(APPEND ALL_SAMPLES pg)
ENDIF (FUSEPP_USE_POSTGRESQL)

FOREACH (mysample ${ALL_SAMPLES})
    ADD_SUBDIRECTORY(${mysample})
ENDFOREACH (mysample)
